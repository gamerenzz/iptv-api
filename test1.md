
1.config/user_subscribe.txt：

负责控制**“从哪里抓取”**（订阅源）。
想多抓取一些源，就在里面加一行 URL。
想减少某些源，就在行首加 # 注释掉，或者直接删掉那一行。


2.config/user_demo.txt：

负责控制**“要保留什么、怎么分类”**（播放列表模板）。
想看什么台，或者想给台归类到哪个分组，就在这里面配置。
不想看哪个台了，直接在这里删掉它。


3.config/demo.txt

原播放列表模板
